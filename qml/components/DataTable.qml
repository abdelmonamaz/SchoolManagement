import QtQuick
import QtQuick.Layouts

// Generic data table - used inline in pages
Item {
    id: control
    // This is a placeholder; tables are built inline in each page
    // for flexibility with custom cell rendering.
}
